using UnityEngine;

public class BasicEnemy : EnemyBase
{
    // Intentionally empty because nothing is supposed to change. This may scale or change later, but for now, it'll just be normal.
    // In this case, this empty script only exists so that there is something to attach.
}
