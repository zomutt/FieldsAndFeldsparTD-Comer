using UnityEngine;
using System.Collections.Generic;
using System.Collections;

/// <summary>
/// The logic for iframes, invulnerability, and anything requiring a coroutine lives here.
/// This is intentional because CastleStats is a plain C# class (a non-MonoBehaviour),
/// which cannot run coroutines or use Unity lifecycle methods.
/// </summary>

